# 制作開始・検証・納品の手順

## 1. 適用と優先順位

対象は準2級Reading。上位指示と今回の明示指定を優先する。準2級プラス・他級・他技能に無言で切り替えない。今回の指定と既定値が違う場合、適用を外す要件と理由を作業用に記録する。

形式の正本は `specs/pre2_reading.json`、意味上の要件は `MASTER_PROMPT.md` と `rules/`、組版は `LAYOUT_MASTER_PROMPT.md`。`references/` は証拠と分析であり、全ての観察を永久の公式規則としない。矛盾を発見したら公式資料で解決し、必要な仕様・テストを同時に更新する。

## 2. 全文を読む

同一版の再読を無限に繰り返す必要はないが、断片取得や記憶で代用しない。切れた出力は続きを読む。

1. 本書と `README.md`
2. `MASTER_PROMPT.md`、`LAYOUT_MASTER_PROMPT.md`、`specs/pre2_reading.json`
3. `rules/DISTRACTOR_DESIGN.md`、`rules/FAMOUS_EPISODE_POLICY.md`、`rules/NATURAL_JAPANESE_TRANSLATION.md`
4. `references/OFFICIAL_PRE2_ANALYSIS.md`、`references/official_pre2_observations.json`、`references/EIKEN_PRE2_LAYOUT_REFERENCE.md`、`references/OFFICIAL_DISTRACTOR_CASEBOOK.md`
5. データを生成・検査する場合は `docs/DATA_CONTRACT.md`。リポジトリ修正時は `docs/DESIGN_REVIEW.md` と対象コード・テストも読む。

取得元・版/SHA・読了範囲を短く作業記録に残す。公開されている最新の公式問題と正答を再確認する。形式が変わっていれば古いプロファイルを「最新準拠」として使わない。取得できなければ参照年度と未確認範囲を明示し、最新照合済みと偽らない。

## 3. 要件を実装・検査へ結びつける

作業用に「要件／適用範囲／実装先／検査方法／状態／最終版の証拠」を記録する。次の6群を漏らさない。

- 構成・問番号・会話単位・空所・語数・英文負荷。
- 出典・媒体・事実と教材用創作の区別・題材との関係。
- 全4肢の成立性・一意解・根拠・別問や前付からの答え漏れ。
- 語彙・解答・解説・完成文和訳の対応と自然さ。
- 最終DOCX/PDFの用紙・字形・改ページ・文字列・全ページ画像。
- 保存先・版・実行した検査と未実行の検査の区別。

「読んだ」「PASSと書いた」「ファイルを保存できた」は意味・レイアウト検証の証拠にならない。長い思考過程でなく、照合可能な仕様と判定の要点を残す。

## 4. 制作のゲート

A：全必読資料と公式を確認 → B：題材・各問の狙い・根拠を設計 → C：英文・4肢・訳を構造化し凍結 → D：機械検査と内容レビュー → E：組版・最終ファイル照合・全ページ画像確認。

Dでは `python3 tools/validate_pack.py PACK.json` を実行。エラー0は必要条件であって完成条件ではない。警告を無視せず、修正または妥当性確認を行う。正答を見る前に全4肢を比較し、その後に意図したキーと照合する。別のAI・人が実際に検査していないのに「独立した複数の検査者」と報告しない。

本文・肢の順序・訳・フォント・DOCXを変更したら、影響範囲を再検査し、PDF・画像・旧判定を更新する。最終版のハッシュと検査対象を対応させる。必須条件の不合格・未確認が残れば、完成版ではなく未確認箇所を示した下書きとして扱う。

## 5. 今回のようなリポジトリ更新だけの依頼

対象と依存規則を読む → 変更意図と実装を照合 → ローカルテスト・相対リンク・差分を確認 → 指定リポジトリへ保存 → コミットと保存内容を再取得して検証する。CI結果は実行後の結果を確認してから報告する。

実際の教材を生成していない場合、DOCX/PDF組版・全ページ画像検査・受験者試行は**今回未実施**。プロンプト更新を、既存教材の修正完了や意味上の完全自動検査と説明しない。

## 6. 公開・納品

生徒用教材に内部QA表、制作指示、語数監査、検査ログを載せない。全文和訳を最終セクションにする。生徒氏名・個人情報・私的授業資料・秘密鍵・公式問題の複製を公開GitHubへ入れない。フォントファイルを配布しない。

文書を増やすだけでは遵守を保証できない。実際に使ったツール、実際に検証した範囲、できなかったことを区別する。
